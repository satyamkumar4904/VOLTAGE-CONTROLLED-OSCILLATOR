# VOLTAGE-CONTROLLED-OSCILLATOR
A Voltage-Controlled Oscillator (VCO) is an electronic circuit that generates a repeating output waveform (such as a sine, square, or triangle wave) whose frequency is directly controlled by an input DC or AC voltage. Changing the control voltage changes the output frequency
